{
    "target":  "request",
    "operation":  "request_secret_allowed",
    "expected_failure_fragments":  [
                                       "request packet secret_policy must not allow committed secrets"
                                   ],
    "set_values":  {
                       "secret_policy.committed_secret_material_allowed":  true
                   }
}
